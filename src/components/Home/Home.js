import React from 'react';
import Header from '../Shared/Header/Header'
import Jobs from '../Jobs/Jobs'
const Home = () => {
    return (
        <div>
            <Header></Header>
            <Jobs></Jobs>
        </div>
    );
};

export default Home;